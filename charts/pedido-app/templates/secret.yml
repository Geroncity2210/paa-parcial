apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  labels:
    app.kubernetes.io/name: db-secret
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  user-password: {{ .Values.postgresql.auth.password}}
  postgres-password: {{ .Values.postgresql.auth.postgresPassword }}