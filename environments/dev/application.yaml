apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pedido-app-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: pedido-app
    app.kubernetes.io/instance: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/Geroncity2210/paa-parcial
    targetRevision: HEAD
    path: charts/pedido-app
    helm:
      valueFiles:
        - values-dev.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: pedido-app-dev

  syncPolicy:
    automated:
      prune: true       # Elimina recursos que ya no están en Git
      selfHeal: true    # Corrige cambios manuales no deseados en el clúster
    syncOptions:
      - CreateNamespace=true    # Crea el namespace si no existe
      - PrunePropagationPolicy=foreground
      - PruneLast=true